# 部署问题修复指南

## 🔴 问题：fatal: not a git repository

### 错误信息
```
fatal: not a git repository (or any of the parent directories): .git
```

### 原因
`/opt/PhoneClone` 目录存在但不是 git 仓库。可能的原因：
1. 目录是手动创建的，不是通过 `git clone` 获取的
2. `.git` 目录被删除或损坏
3. 从其他地方复制了文件，但没有包含 `.git` 目录

## ✅ 解决方案

### 方案1：删除目录，重新克隆（推荐）

```bash
# 1. 停止服务（如果正在运行）
systemctl stop phoneclone-backend 2>/dev/null || true

# 2. 备份配置文件（如果有）
if [ -f "/opt/PhoneClone/backend/application-production.yml" ]; then
    cp /opt/PhoneClone/backend/application-production.yml /tmp/application-production.yml.backup
    echo "配置文件已备份到 /tmp/application-production.yml.backup"
fi

# 3. 删除旧目录
rm -rf /opt/PhoneClone

# 4. 重新克隆
git clone git@github.com:huiyiyouck/PhoneClone.git /opt/PhoneClone

# 5. 恢复配置文件（如果有备份）
if [ -f "/tmp/application-production.yml.backup" ]; then
    cp /tmp/application-production.yml.backup /opt/PhoneClone/backend/application-production.yml
    echo "配置文件已恢复"
fi

# 6. 修改部署脚本中的仓库地址（如果需要）
cd /opt/PhoneClone
nano 阿里云自动部署脚本.sh
# 找到 GITHUB_REPO 这一行，确保是正确的地址：
# GITHUB_REPO="git@github.com:huiyiyouck/PhoneClone.git"

# 7. 运行部署脚本
./阿里云自动部署脚本.sh
```

### 方案2：在当前目录初始化git仓库

```bash
cd /opt/PhoneClone

# 1. 初始化git仓库
git init

# 2. 添加远程仓库
git remote add origin git@github.com:huiyiyouck/PhoneClone.git

# 3. 拉取代码
git fetch origin
git branch -M main
git reset --hard origin/main

# 4. 运行部署脚本
./阿里云自动部署脚本.sh
```

### 方案3：使用修复后的部署脚本（已更新）

部署脚本已经更新，会自动检测并处理这种情况。如果目录存在但不是git仓库，脚本会：
1. 备份现有目录
2. 重新克隆代码
3. 继续部署流程

**使用修复后的脚本**：

```bash
# 1. 从GitHub拉取最新脚本（如果已更新）
cd /opt/PhoneClone
git pull origin main

# 或手动更新脚本内容

# 2. 运行部署脚本
./阿里云自动部署脚本.sh
```

## 🔧 完整修复步骤（推荐）

### 步骤1：检查当前状态

```bash
# 检查目录是否存在
ls -la /opt/PhoneClone

# 检查是否是git仓库
cd /opt/PhoneClone
ls -la .git
```

### 步骤2：备份重要文件

```bash
# 备份配置文件
if [ -f "/opt/PhoneClone/backend/application-production.yml" ]; then
    mkdir -p /tmp/phoneclone_backup
    cp /opt/PhoneClone/backend/application-production.yml /tmp/phoneclone_backup/
    echo "配置文件已备份"
fi
```

### 步骤3：清理并重新克隆

```bash
# 停止服务
systemctl stop phoneclone-backend 2>/dev/null || true

# 删除旧目录
rm -rf /opt/PhoneClone

# 重新克隆
git clone git@github.com:huiyiyouck/PhoneClone.git /opt/PhoneClone

# 验证克隆成功
cd /opt/PhoneClone
ls -la
git status
```

### 步骤4：恢复配置文件

```bash
# 如果有备份，恢复配置文件
if [ -f "/tmp/phoneclone_backup/application-production.yml" ]; then
    cp /tmp/phoneclone_backup/application-production.yml /opt/PhoneClone/backend/application-production.yml
    echo "配置文件已恢复"
else
    echo "需要创建配置文件"
    nano /opt/PhoneClone/backend/application-production.yml
fi
```

### 步骤5：配置部署脚本

```bash
cd /opt/PhoneClone

# 编辑部署脚本，确保GitHub仓库地址正确
nano 阿里云自动部署脚本.sh

# 找到这一行，确保地址正确：
# GITHUB_REPO="git@github.com:huiyiyouck/PhoneClone.git"
```

### 步骤6：运行部署脚本

```bash
# 给脚本添加执行权限
chmod +x 阿里云自动部署脚本.sh

# 运行部署
./阿里云自动部署脚本.sh
```

## 📋 快速修复命令（一键执行）

```bash
#!/bin/bash
# 一键修复脚本

echo "开始修复..."

# 停止服务
systemctl stop phoneclone-backend 2>/dev/null || true

# 备份配置
if [ -f "/opt/PhoneClone/backend/application-production.yml" ]; then
    cp /opt/PhoneClone/backend/application-production.yml /tmp/app-config-backup.yml
    echo "✓ 配置文件已备份"
fi

# 删除旧目录
rm -rf /opt/PhoneClone
echo "✓ 旧目录已删除"

# 重新克隆
echo "正在克隆代码..."
git clone git@github.com:huiyiyouck/PhoneClone.git /opt/PhoneClone

if [ $? -eq 0 ]; then
    echo "✓ 代码克隆成功"
    
    # 恢复配置
    if [ -f "/tmp/app-config-backup.yml" ]; then
        cp /tmp/app-config-backup.yml /opt/PhoneClone/backend/application-production.yml
        echo "✓ 配置文件已恢复"
    fi
    
    # 进入目录
    cd /opt/PhoneClone
    
    # 运行部署
    echo "开始部署..."
    chmod +x 阿里云自动部署脚本.sh
    ./阿里云自动部署脚本.sh
else
    echo "✗ 克隆失败，请检查SSH密钥配置"
    exit 1
fi
```

## ⚠️ 注意事项

1. **配置文件备份**：重新克隆前一定要备份 `application-production.yml`，因为包含数据库密码等敏感信息

2. **SSH密钥**：确保SSH密钥已正确配置，否则克隆会失败

3. **目录权限**：确保 `/opt` 目录有写入权限

4. **服务状态**：部署前建议停止旧服务

## ✅ 验证修复成功

修复后，运行以下命令验证：

```bash
# 1. 检查是否是git仓库
cd /opt/PhoneClone
git status

# 2. 检查文件是否完整
ls -la backend/

# 3. 检查部署脚本
ls -la 阿里云自动部署脚本.sh

# 4. 运行部署脚本
./阿里云自动部署脚本.sh
```

---

**总结**：最简单的方法是删除 `/opt/PhoneClone` 目录，然后重新从GitHub克隆。

