# 快速修复命令 - fatal: not a git repository

## 🔴 问题
```
fatal: not a git repository (or any of the parent directories): .git
```

## ✅ 快速修复（在ECS服务器上执行）

### 方法1：删除目录，重新克隆（最简单）

```bash
# 1. 备份配置文件（如果有）
if [ -f "/opt/PhoneClone/backend/application-production.yml" ]; then
    cp /opt/PhoneClone/backend/application-production.yml /tmp/app-config-backup.yml
    echo "配置文件已备份到 /tmp/app-config-backup.yml"
fi

# 2. 停止服务
systemctl stop phoneclone-backend 2>/dev/null || true

# 3. 删除旧目录
rm -rf /opt/PhoneClone

# 4. 重新克隆（注意：使用小写 phoneclone，与脚本一致）
git clone git@github.com:huiyiyouck/PhoneClone.git /opt/phoneclone

# 5. 恢复配置文件
if [ -f "/tmp/app-config-backup.yml" ]; then
    cp /tmp/app-config-backup.yml /opt/phoneclone/backend/application-production.yml
    echo "配置文件已恢复"
fi

# 6. 进入目录并运行部署脚本
cd /opt/phoneclone
chmod +x 阿里云自动部署脚本.sh
./阿里云自动部署脚本.sh
```

### 方法2：在当前目录初始化git（保留现有文件）

```bash
cd /opt/PhoneClone

# 1. 初始化git仓库
git init

# 2. 添加远程仓库
git remote add origin git@github.com:huiyiyouck/PhoneClone.git

# 3. 拉取代码（会覆盖本地文件）
git fetch origin
git branch -M main
git reset --hard origin/main

# 4. 运行部署脚本
chmod +x 阿里云自动部署脚本.sh
./阿里云自动部署脚本.sh
```

### 方法3：使用修复后的脚本（推荐）

部署脚本已更新，会自动检测并修复。只需：

```bash
# 1. 确保在正确的目录（注意大小写）
cd /opt/PhoneClone

# 2. 从GitHub拉取最新脚本（如果已更新）
# 或者手动更新脚本中的 update_code 函数

# 3. 运行部署脚本
./阿里云自动部署脚本.sh
```

## ⚠️ 重要提示

### 目录名称问题

部署脚本中配置的是：
```bash
APP_DIR="/opt/phoneclone"  # 小写
```

但你当前使用的目录是：
```bash
/opt/PhoneClone  # 大写P
```

**建议**：
1. **统一使用小写**（推荐）：
   ```bash
   # 删除大写目录
   rm -rf /opt/PhoneClone
   
   # 克隆到小写目录
   git clone git@github.com:huiyiyouck/PhoneClone.git /opt/phoneclone
   ```

2. **或修改脚本使用大写**：
   ```bash
   # 编辑脚本
   nano /opt/PhoneClone/阿里云自动部署脚本.sh
   
   # 修改这一行：
   # APP_DIR="/opt/PhoneClone"  # 改为大写
   ```

## 📋 完整修复步骤（推荐）

```bash
# ===== 步骤1：备份配置 =====
if [ -f "/opt/PhoneClone/backend/application-production.yml" ]; then
    mkdir -p /tmp/phoneclone_backup
    cp /opt/PhoneClone/backend/application-production.yml /tmp/phoneclone_backup/
    echo "✓ 配置文件已备份"
fi

# ===== 步骤2：停止服务 =====
systemctl stop phoneclone-backend 2>/dev/null || true
echo "✓ 服务已停止"

# ===== 步骤3：清理旧目录 =====
rm -rf /opt/PhoneClone
rm -rf /opt/phoneclone
echo "✓ 旧目录已清理"

# ===== 步骤4：重新克隆 =====
git clone git@github.com:huiyiyouck/PhoneClone.git /opt/phoneclone
echo "✓ 代码已克隆"

# ===== 步骤5：恢复配置 =====
if [ -f "/tmp/phoneclone_backup/application-production.yml" ]; then
    cp /tmp/phoneclone_backup/application-production.yml /opt/phoneclone/backend/application-production.yml
    echo "✓ 配置文件已恢复"
else
    echo "⚠ 需要创建配置文件"
    echo "请运行: nano /opt/phoneclone/backend/application-production.yml"
fi

# ===== 步骤6：运行部署 =====
cd /opt/phoneclone
chmod +x 阿里云自动部署脚本.sh
./阿里云自动部署脚本.sh
```

## ✅ 验证修复

```bash
# 1. 检查是否是git仓库
cd /opt/phoneclone
git status

# 应该看到类似输出：
# On branch main
# Your branch is up to date with 'origin/main'.

# 2. 检查文件
ls -la backend/

# 3. 检查部署脚本
ls -la 阿里云自动部署脚本.sh
```

---

**最简单的方法**：删除 `/opt/PhoneClone`，然后重新克隆到 `/opt/phoneclone`（小写）。

