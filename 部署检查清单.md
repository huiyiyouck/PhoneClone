# 部署检查清单

## ✅ Supabase数据库部署

- [ ] 创建Supabase账号和项目
- [ ] 记录数据库连接信息（Host, Port, Database, User, Password）
- [ ] 执行数据库初始化脚本（V1__init.sql）
- [ ] 创建测试账号（V2__create_test_users.sql）
- [ ] 测试数据库连接（psql或SQL Editor）
- [ ] 配置连接池（可选）

## ✅ Railway/Render后端部署

- [ ] 创建Railway/Render账号
- [ ] 连接GitHub仓库
- [ ] 配置构建命令：`./gradlew build -x test`
- [ ] 配置启动命令：`java -jar build/libs/phoneclone-backend-1.0.0.jar`
- [ ] 设置根目录：`backend`
- [ ] 配置环境变量：
  - [ ] `SPRING_DATASOURCE_URL`（Supabase JDBC连接字符串）
  - [ ] `DB_USERNAME`（postgres）
  - [ ] `DB_PASSWORD`（Supabase密码）
  - [ ] `JWT_SECRET`（至少32字符的随机字符串）
  - [ ] `SPRING_PROFILES_ACTIVE=production`
  - [ ] `PORT=8080`
- [ ] 等待部署完成
- [ ] 获取部署URL
- [ ] 测试API接口
- [ ] 访问Swagger UI验证

## ✅ Android应用配置

- [ ] 更新 `RetrofitClient.java` 中的API地址
- [ ] 构建Release APK
- [ ] 安装并测试应用
- [ ] 使用测试账号登录验证

## ✅ 功能测试

- [ ] 用户注册功能
- [ ] 用户登录功能
- [ ] 应用列表功能
- [ ] 数据同步功能
- [ ] 多开实例功能（如已实现）

## ✅ 安全检查

- [ ] 确认数据库密码强度
- [ ] 确认JWT密钥足够长（至少32字符）
- [ ] 确认使用HTTPS（Railway/Render自动提供）
- [ ] 确认环境变量未暴露在代码中
- [ ] 检查API访问日志

## ✅ 监控设置

- [ ] 配置Railway/Render监控
- [ ] 设置Supabase监控
- [ ] 配置日志查看
- [ ] 设置告警（如需要）

## 环境变量模板

### Railway/Render环境变量

```bash
# 数据库配置（Supabase）
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=your-supabase-password

# JWT配置
JWT_SECRET=your-256-bit-secret-key-minimum-32-characters-long-random-string

# Spring配置
SPRING_PROFILES_ACTIVE=production
PORT=8080
```

### 生成JWT密钥

```bash
# 使用openssl生成随机密钥
openssl rand -base64 32

# 或使用在线工具
# https://www.random.org/strings/
```

## 测试命令

### 测试数据库连接

```bash
psql "postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres"
```

### 测试后端API

```bash
# 注册
curl -X POST https://your-app.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"12345678"}'

# 登录
curl -X POST https://your-app.railway.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'
```

### 访问Swagger UI

```
https://your-app.railway.app/swagger-ui.html
```

## 常见问题

### Q1: Railway部署失败

**检查**：
- [ ] Java版本是否正确（需要Java 21）
- [ ] 构建命令是否正确
- [ ] 环境变量是否配置
- [ ] 查看构建日志

### Q2: 数据库连接失败

**检查**：
- [ ] Supabase连接信息是否正确
- [ ] SSL配置（`sslmode=require`）
- [ ] 密码是否正确
- [ ] 防火墙规则

### Q3: API返回404

**检查**：
- [ ] API地址是否正确（包含 `/api` 前缀）
- [ ] 后端服务是否运行
- [ ] 路由配置是否正确

### Q4: Android应用无法连接

**检查**：
- [ ] API地址配置
- [ ] 网络权限
- [ ] HTTPS证书
- [ ] 后端服务状态

## 完成标志

当所有检查项都完成，并且：
- ✅ 可以在Swagger UI中看到API文档
- ✅ 可以使用curl测试API
- ✅ Android应用可以正常登录
- ✅ 数据可以正常同步

**部署完成！** 🎉

