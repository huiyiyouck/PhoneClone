# 阿里云部署检查清单

## ✅ ECS实例准备

- [ ] 购买ECS实例（1核2GB或更高）
- [ ] 选择Ubuntu 22.04或CentOS 7.9镜像
- [ ] 分配公网IP
- [ ] 设置root密码或SSH密钥
- [ ] 配置安全组（开放22、8080端口）

## ✅ 服务器配置

- [ ] SSH连接到服务器
- [ ] 更新系统：`apt update && apt upgrade -y`
- [ ] 安装Java 21：`apt install -y openjdk-21-jdk`
- [ ] 验证Java版本：`java -version`

## ✅ 应用部署

- [ ] 本地构建JAR：`cd backend && ./gradlew clean build -x test`
- [ ] 上传JAR到服务器：`scp build/libs/phoneclone-backend-1.0.0.jar root@IP:/opt/phoneclone/`
- [ ] 创建应用目录：`mkdir -p /opt/phoneclone`
- [ ] 创建配置文件：`application-production.yml`
- [ ] 配置数据库连接（Supabase）
- [ ] 配置JWT密钥

## ✅ 服务管理

- [ ] 创建systemd服务文件
- [ ] 启动服务：`sudo systemctl start phoneclone-backend`
- [ ] 设置开机自启：`sudo systemctl enable phoneclone-backend`
- [ ] 检查服务状态：`sudo systemctl status phoneclone-backend`
- [ ] 查看日志：`sudo journalctl -u phoneclone-backend -f`

## ✅ 网络配置

- [ ] 配置安全组（开放8080端口）
- [ ] 配置防火墙（如使用）
- [ ] 测试本地访问：`curl http://localhost:8080/api/auth/login`
- [ ] 测试公网访问：`curl http://你的IP:8080/api/auth/login`

## ✅ Nginx配置（可选）

- [ ] 安装Nginx
- [ ] 创建Nginx配置
- [ ] 测试配置：`nginx -t`
- [ ] 重启Nginx：`systemctl restart nginx`
- [ ] 测试通过Nginx访问

## ✅ HTTPS配置（可选）

- [ ] 绑定域名到ECS IP
- [ ] 安装Certbot
- [ ] 申请SSL证书
- [ ] 配置Nginx SSL
- [ ] 测试HTTPS访问

## ✅ 验证部署

- [ ] 测试注册API
- [ ] 测试登录API
- [ ] 访问Swagger UI
- [ ] 测试Android应用连接

## 环境变量检查

确认以下配置正确：

```bash
# 数据库配置
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=你的Supabase密码

# JWT配置
JWT_SECRET=至少32字符的随机字符串

# Spring配置
SPRING_PROFILES_ACTIVE=production
PORT=8080
```

## 测试命令

```bash
# 1. 测试服务状态
sudo systemctl status phoneclone-backend

# 2. 测试API
curl -X POST http://你的IP:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'

# 3. 访问Swagger
# 浏览器打开：http://你的IP:8080/swagger-ui.html
```

## 完成标志

当所有检查项完成，并且：
- ✅ 服务运行正常
- ✅ API可以访问
- ✅ Android应用可以连接
- ✅ 数据可以正常同步

**部署完成！** 🎉

