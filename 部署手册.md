# 部署手册 - 混合架构方案

## 架构说明

本应用采用**混合架构**：
- **Android应用**：客户端 + 轻量级本地服务（仅处理本地数据）
- **Spring Boot后端服务**：处理业务逻辑、认证、数据同步
- **PostgreSQL云数据库**：数据存储（支持多设备同步）

```
┌─────────────────┐
│  Android客户端   │
│  (HTTP API调用)  │
└────────┬────────┘
         │ HTTP/REST
┌────────▼────────┐
│  Spring Boot     │
│  后端API服务     │
└────────┬────────┘
         │ JDBC
┌────────▼────────┐
│  PostgreSQL      │
│  云数据库        │
└─────────────────┘
```

## 部署步骤

### 第一步：部署PostgreSQL数据库

#### 方案A：使用云数据库服务（推荐）

**阿里云RDS PostgreSQL**：
1. 登录阿里云控制台
2. 创建RDS PostgreSQL实例
3. 配置白名单（允许后端服务器IP）
4. 创建数据库：`phoneclone_db`
5. 记录连接信息

**腾讯云PostgreSQL**：
1. 登录腾讯云控制台
2. 创建PostgreSQL实例
3. 配置安全组（开放5432端口）
4. 创建数据库：`phoneclone_db`

**AWS RDS PostgreSQL**：
1. 登录AWS控制台
2. 创建RDS PostgreSQL实例
3. 配置安全组
4. 创建数据库：`phoneclone_db`

#### 方案B：自建PostgreSQL服务器

参考之前的部署手册中的PostgreSQL配置步骤。

### 第二步：部署Spring Boot后端服务

#### 1. 配置数据库连接

编辑 `backend/src/main/resources/application.yml`:

```yaml
spring:
  datasource:
    url: jdbc:postgresql://your-db-host:5432/phoneclone_db
    username: your_username
    password: your_password
```

#### 2. 配置JWT密钥

```yaml
spring:
  security:
    jwt:
      secret: your-256-bit-secret-key-change-this-in-production
```

#### 3. 配置服务器端口（如需要）

```yaml
server:
  port: 8080
```

#### 4. 构建后端服务

```bash
cd backend
./gradlew clean build
./gradlew bootJar
```

生成的JAR文件：`backend/build/libs/phoneclone-backend-1.0.0.jar`

#### 5. 运行后端服务

**本地运行**：
```bash
java -jar backend/build/libs/phoneclone-backend-1.0.0.jar
```

**服务器部署**（使用systemd）：

创建服务文件 `/etc/systemd/system/phoneclone-backend.service`:

```ini
[Unit]
Description=PhoneClone Backend Service
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/opt/phoneclone
ExecStart=/usr/bin/java -jar /opt/phoneclone/phoneclone-backend-1.0.0.jar
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable phoneclone-backend
sudo systemctl start phoneclone-backend
sudo systemctl status phoneclone-backend
```

**Docker部署**：

创建 `backend/Dockerfile`:
```dockerfile
FROM openjdk:21-jdk-slim
WORKDIR /app
COPY build/libs/phoneclone-backend-1.0.0.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

构建和运行：
```bash
cd backend
docker build -t phoneclone-backend .
docker run -d -p 8080:8080 --name phoneclone-backend phoneclone-backend
```

### 第三步：初始化数据库

#### 方法1：自动迁移（推荐）

Spring Boot启动时会自动执行Flyway迁移脚本：
- `backend/src/main/resources/db/migration/V1__init.sql` - 创建表结构
- `backend/src/main/resources/db/migration/V2__create_test_users.sql` - 创建测试账号

#### 方法2：手动执行SQL

```bash
psql -h your-db-host -U your_username -d phoneclone_db -f backend/src/main/resources/db/migration/V1__init.sql
psql -h your-db-host -U your_username -d phoneclone_db -f backend/src/main/resources/db/migration/V2__create_test_users.sql
```

### 第四步：配置Android应用

#### 1. 配置API地址

编辑 `app/src/main/java/com/phoneclone/network/RetrofitClient.java`:

```java
// 生产环境API地址
private static final String BASE_URL = "https://your-api-domain.com/api/";

// 开发环境（本地）
// private static final String BASE_URL = "http://10.0.2.2:8080/api/";  // 模拟器
// private static final String BASE_URL = "http://192.168.1.100:8080/api/";  // 真机
```

#### 2. 构建APK

```bash
cd app
./gradlew assembleRelease

# APK位置：app/build/outputs/apk/release/app-release.apk
```

### 第五步：验证部署

#### 1. 测试后端服务

```bash
# 检查服务是否运行
curl http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"12345678"}'

# 访问Swagger UI
# http://your-server:8080/swagger-ui.html
```

#### 2. 测试Android应用

1. 安装APK
2. 打开应用
3. 使用测试账号登录
4. 测试各项功能

## 测试账号

### 测试账号列表

| 用户名 | 邮箱 | 密码 | 会员等级 |
|--------|------|------|----------|
| testuser | test@example.com | 12345678 | FREE |
| vipuser | vip@example.com | 12345678 | VIP |
| admin | admin@example.com | admin123 | SVIP |

这些账号会在执行数据库迁移脚本 `V2__create_test_users.sql` 时自动创建。

## 安全建议

### 1. 后端服务安全

- ✅ 使用HTTPS（配置SSL证书）
- ✅ 配置防火墙规则
- ✅ 使用反向代理（Nginx）
- ✅ 定期更新依赖
- ✅ 启用日志监控

### 2. 数据库安全

- ✅ 使用强密码
- ✅ 限制访问IP（白名单）
- ✅ 使用SSL连接
- ✅ 定期备份
- ✅ 监控异常访问

### 3. Android应用安全

- ✅ 使用HTTPS通信
- ✅ ProGuard代码混淆
- ✅ 证书固定
- ✅ 安全存储敏感信息

## Nginx反向代理配置（可选）

创建 `/etc/nginx/sites-available/phoneclone`:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/phoneclone /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

## 监控和维护

### 查看后端日志

```bash
# systemd服务
sudo journalctl -u phoneclone-backend -f

# Docker容器
docker logs -f phoneclone-backend
```

### 数据库备份

```bash
# 每日备份
pg_dump -h your-db-host -U your_username -d phoneclone_db > backup_$(date +%Y%m%d).sql

# 恢复
psql -h your-db-host -U your_username -d phoneclone_db < backup_20240101.sql
```

### 性能监控

- 使用Spring Boot Actuator监控应用健康状态
- 监控数据库连接池
- 监控API响应时间

## 故障排查

### 后端服务无法启动

1. 检查Java版本：`java -version`（需要Java 21）
2. 检查端口是否被占用：`netstat -tulpn | grep 8080`
3. 检查数据库连接：查看日志中的错误信息
4. 检查配置文件：`application.yml`

### Android应用无法连接后端

1. 检查后端服务是否运行
2. 检查API地址配置
3. 检查网络连接
4. 检查防火墙规则
5. 查看Android Logcat日志

### 数据库连接失败

1. 检查数据库是否运行
2. 检查连接信息是否正确
3. 检查网络是否可达
4. 检查防火墙和白名单配置
