# 服务启动问题排查

## 🔴 问题现象

- 服务状态显示：`active (running)`
- 但8080端口未监听：`netstat -tlnp | grep 8080` 无输出

这说明服务虽然启动了，但可能启动失败或未正常监听端口。

## ✅ 排查步骤

### 第一步：查看详细日志

```bash
# 查看最近100行日志
journalctl -u phoneclone-backend -n 100 --no-pager

# 实时查看日志
journalctl -u phoneclone-backend -f

# 查看错误日志
journalctl -u phoneclone-backend -p err --no-pager
```

### 第二步：检查JAR文件是否存在

```bash
# 检查JAR文件
ls -lh /opt/phoneclone/backend/build/libs/phoneclone-backend-1.0.0.jar

# 如果不存在，需要重新构建
cd /opt/phoneclone/backend
./gradlew clean build -x test
```

### 第三步：手动运行JAR文件测试

```bash
# 停止systemd服务
systemctl stop phoneclone-backend

# 手动运行，查看详细错误
cd /opt/phoneclone/backend
java -Xmx512m -Xms256m -jar build/libs/phoneclone-backend-1.0.0.jar \
  --spring.config.location=classpath:/application.yml,file:application-production.yml \
  --spring.profiles.active=production
```

这样可以直观看到启动错误信息。

### 第四步：检查配置文件

```bash
# 检查配置文件是否存在
ls -la /opt/phoneclone/backend/application-production.yml

# 查看配置文件内容（注意隐藏敏感信息）
cat /opt/phoneclone/backend/application-production.yml | grep -v password | grep -v secret
```

### 第五步：检查数据库连接

配置文件中的数据库连接可能有问题。检查：

```bash
# 查看数据库配置
cat /opt/phoneclone/backend/application-production.yml | grep -A 5 datasource
```

## 🔧 常见问题及解决

### 问题1：配置文件格式错误

**检查YAML格式**：
```bash
# 验证YAML格式
python3 -c "import yaml; yaml.safe_load(open('/opt/phoneclone/backend/application-production.yml'))"
```

**常见错误**：
- 缩进错误（YAML对缩进敏感）
- 缺少冒号
- 引号不匹配

### 问题2：数据库连接失败

**测试数据库连接**：
```bash
# 安装PostgreSQL客户端（如果没有）
apt install -y postgresql-client

# 测试连接（替换为实际配置）
psql -h db.xxxxx.supabase.co -U postgres -d postgres
```

**检查配置**：
- URL格式是否正确
- 密码是否正确
- SSL模式是否正确（`sslmode=require`）

### 问题3：JWT密钥格式错误

**检查JWT密钥**：
- 密钥长度至少32字符
- 不能包含特殊字符导致YAML解析错误

### 问题4：端口被占用

```bash
# 检查8080端口是否被占用
lsof -i :8080
netstat -tlnp | grep 8080

# 如果被占用，找到进程并停止
kill -9 [PID]
```

### 问题5：Java版本问题

```bash
# 检查Java版本
java -version

# 应该是Java 21
# 如果不是，安装Java 21
apt install -y openjdk-21-jdk
update-alternatives --config java
```

## 📋 完整排查命令

```bash
#!/bin/bash
echo "=== 1. 检查服务状态 ==="
systemctl status phoneclone-backend --no-pager -l

echo ""
echo "=== 2. 查看最新日志 ==="
journalctl -u phoneclone-backend -n 50 --no-pager

echo ""
echo "=== 3. 检查JAR文件 ==="
ls -lh /opt/phoneclone/backend/build/libs/phoneclone-backend-1.0.0.jar

echo ""
echo "=== 4. 检查端口监听 ==="
netstat -tlnp | grep 8080

echo ""
echo "=== 5. 检查配置文件 ==="
ls -la /opt/phoneclone/backend/application-production.yml

echo ""
echo "=== 6. 检查Java版本 ==="
java -version
```

## 🎯 快速修复

如果服务启动失败，尝试：

```bash
# 1. 停止服务
systemctl stop phoneclone-backend

# 2. 查看错误日志
journalctl -u phoneclone-backend -n 100 --no-pager | tail -50

# 3. 手动运行查看错误
cd /opt/phoneclone/backend
java -jar build/libs/phoneclone-backend-1.0.0.jar \
  --spring.config.location=classpath:/application.yml,file:application-production.yml \
  --spring.profiles.active=production

# 4. 根据错误信息修复问题

# 5. 重新启动服务
systemctl start phoneclone-backend
```

---

**请先执行 `journalctl -u phoneclone-backend -n 100 --no-pager` 查看详细日志，然后根据错误信息进行修复。**

