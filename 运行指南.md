# 项目运行指南

## 前置要求

### Android客户端
- Android Studio (最新版本)
- JDK 21
- Android SDK 29+
- Gradle 8.5+

### 后端服务
- JDK 21
- PostgreSQL 12+
- Gradle 8.5+ (或使用项目自带的gradle wrapper)

## 第一步：配置后端服务

### 1. 创建PostgreSQL数据库

```sql
-- 连接到PostgreSQL
CREATE DATABASE phoneclone_db;
```

### 2. 配置数据库连接

编辑 `backend/src/main/resources/application.yml`:

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phoneclone_db
    username: your_username  # 替换为你的PostgreSQL用户名
    password: your_password  # 替换为你的PostgreSQL密码
```

### 3. 配置JWT密钥

在 `application.yml` 中设置JWT密钥（或使用环境变量）：

```yaml
spring:
  security:
    jwt:
      secret: your-256-bit-secret-key-change-this-in-production  # 至少32个字符
```

### 4. 启动后端服务

```bash
cd backend
./gradlew bootRun
```

或者使用IDE直接运行 `PhoneCloneBackendApplication.java`

后端服务将在 `http://localhost:8080` 启动

### 5. 验证后端服务

访问 Swagger UI: http://localhost:8080/swagger-ui.html

或使用curl测试：

```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"12345678"}'
```

## 第二步：配置Android客户端

### 1. 配置API地址

编辑 `app/src/main/java/com/phoneclone/network/RetrofitClient.java`:

```java
// 如果是Android模拟器访问本地后端，使用：
private static final String BASE_URL = "http://10.0.2.2:8080/api/";

// 如果是真机访问，使用电脑的IP地址：
// private static final String BASE_URL = "http://192.168.1.100:8080/api/";
```

### 2. 同步项目

在Android Studio中：
1. 打开项目
2. 等待Gradle同步完成
3. 如果遇到依赖问题，点击 "Sync Project with Gradle Files"

### 3. 运行应用

1. 连接Android设备或启动模拟器（API 29+）
2. 点击 "Run" 按钮或按 `Shift+F10`
3. 应用将安装并启动

## 第三步：测试功能

### 1. 注册账号
- 打开应用
- 点击"立即注册"
- 填写信息并注册

### 2. 登录
- 使用注册的邮箱和密码登录

### 3. 添加应用
- 点击"添加应用"
- 选择要多开的应用
- 点击右下角"+"按钮

## 常见问题

### 后端启动失败

**问题**: 数据库连接失败
- 检查PostgreSQL是否运行
- 检查数据库用户名密码是否正确
- 检查数据库是否已创建

**问题**: 端口被占用
- 修改 `application.yml` 中的 `server.port`

### Android客户端连接失败

**问题**: 无法连接到后端
- 检查后端服务是否运行
- 检查API地址配置是否正确
- 如果是真机，确保手机和电脑在同一网络
- 检查防火墙设置

**问题**: 网络权限错误
- 确保 `AndroidManifest.xml` 中已添加网络权限

### 编译错误

**问题**: Hilt相关错误
- 确保所有Activity都正确配置
- 清理并重新构建项目：`Build > Clean Project` 然后 `Build > Rebuild Project`

**问题**: Room数据库错误
- 确保数据库版本号正确
- 如果修改了实体类，需要增加版本号

## 开发模式说明

### VirtualApp SDK集成

当前多开功能使用占位实现。要启用完整功能：

1. 获取VirtualApp SDK（AAR文件）
2. 将AAR文件放入 `app/libs/` 目录
3. 在 `app/build.gradle` 中添加：
   ```gradle
   dependencies {
       implementation files('libs/virtualapp.aar')
   }
   ```
4. 在 `MultiInstanceService.java` 中实现具体功能

### 数据同步

当前数据同步功能已搭建框架，完整实现需要：
1. 实现 `SyncRepository` 中的同步逻辑
2. 创建应用和实例的API接口调用
3. 处理冲突和错误情况

## 下一步开发

1. 完善多开功能（集成VirtualApp SDK）
2. 实现完整的数据同步逻辑
3. 添加应用实例管理UI
4. 优化用户体验
5. 添加错误处理和用户提示

## 技术支持

如遇到问题，请检查：
1. 日志输出（Android Logcat 和 后端控制台）
2. 网络连接
3. 数据库状态
4. 配置文件是否正确

