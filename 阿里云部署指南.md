# 阿里云部署指南 - Spring Boot后端服务

## 部署方案选择

阿里云提供多种部署Spring Boot的方案：

### 方案A：ECS云服务器（推荐，灵活可控）
- ✅ 完全控制
- ✅ 适合生产环境
- ✅ 支持自定义配置
- ⚠️ 需要自行维护

### 方案B：容器服务ACK（推荐，容器化）
- ✅ 容器化部署
- ✅ 自动扩缩容
- ✅ 易于管理
- ⚠️ 需要Docker知识

### 方案C：函数计算FC（不推荐）
- ⚠️ 对Spring Boot支持有限
- ⚠️ 需要重构代码

## 方案A：ECS云服务器部署（推荐）

### 第一步：购买和配置ECS实例

#### 1. 购买ECS实例

1. 登录 [阿里云控制台](https://ecs.console.aliyun.com)
2. 点击 "创建实例"
3. 配置实例：
   - **地域**：选择离你最近的区域
   - **实例规格**：
     - 最低配置：1核2GB（测试用）
     - 推荐配置：2核4GB（生产环境）
   - **镜像**：选择 "Ubuntu 22.04" 或 "CentOS 7.9"
   - **系统盘**：40GB SSD
   - **网络**：专有网络VPC
   - **公网IP**：分配公网IP
   - **安全组**：开放端口 8080（HTTP）和 443（HTTPS）
4. 设置登录密码或SSH密钥
5. 点击 "创建实例"

#### 2. 配置安全组

1. 进入ECS实例 → "安全组"
2. 点击 "配置规则"
3. 添加规则：
   - **端口范围**：8080/8080
   - **授权对象**：0.0.0.0/0（允许所有IP访问）
   - **协议类型**：TCP
   - **描述**：Spring Boot应用端口

### 第二步：连接ECS服务器

#### 使用SSH连接

```bash
# 使用密码登录
ssh root@你的ECS公网IP

# 或使用密钥
ssh -i your-key.pem root@你的ECS公网IP
```

#### 使用阿里云Web终端

1. 在ECS控制台点击 "远程连接"
2. 选择 "Workbench远程连接"
3. 输入密码登录

### 第三步：安装Java 21

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y openjdk-21-jdk

# 验证安装
java -version
# 应该显示：openjdk version "21"

# CentOS/RHEL
sudo yum install -y java-21-openjdk java-21-openjdk-devel

# 验证安装
java -version
```

### 第四步：上传应用文件

#### 方法1：使用SCP上传

在本地执行：

```bash
# 先构建JAR文件
cd backend
./gradlew clean build -x test

# 上传JAR文件到服务器
scp build/libs/phoneclone-backend-1.0.0.jar root@你的ECS公网IP:/opt/phoneclone/

# 上传配置文件（可选）
scp src/main/resources/application-production.yml root@你的ECS公网IP:/opt/phoneclone/
```

#### 方法2：使用Git克隆

在服务器上执行：

```bash
# 安装Git
sudo apt install -y git  # Ubuntu
# 或
sudo yum install -y git  # CentOS

# 克隆项目
cd /opt
git clone 你的项目仓库地址 phoneclone
cd phoneclone/backend

# 构建项目
./gradlew clean build -x test
```

### 第五步：配置应用

#### 1. 创建应用目录

```bash
sudo mkdir -p /opt/phoneclone
sudo chown $USER:$USER /opt/phoneclone
cd /opt/phoneclone
```

#### 2. 创建配置文件

创建 `application-production.yml`:

```bash
nano application-production.yml
```

内容：

```yaml
spring:
  application:
    name: phoneclone-backend
  
  datasource:
    url: jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 你的Supabase密码
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  
  security:
    jwt:
      secret: 你的JWT密钥至少32个字符
      expiration: 86400000

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.phoneclone: INFO
    org.springframework.security: WARN
```

保存：`Ctrl+O`，回车，`Ctrl+X`

#### 3. 创建启动脚本

创建 `start.sh`:

```bash
nano start.sh
```

内容：

```bash
#!/bin/bash
cd /opt/phoneclone
java -Xmx512m -Xms256m -jar phoneclone-backend-1.0.0.jar \
  --spring.config.location=classpath:/application.yml,file:./application-production.yml \
  --spring.profiles.active=production
```

设置执行权限：

```bash
chmod +x start.sh
```

### 第六步：使用systemd管理服务

#### 1. 创建systemd服务文件

```bash
sudo nano /etc/systemd/system/phoneclone-backend.service
```

内容：

```ini
[Unit]
Description=PhoneClone Backend Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/phoneclone
ExecStart=/usr/bin/java -Xmx512m -Xms256m -jar /opt/phoneclone/phoneclone-backend-1.0.0.jar --spring.config.location=classpath:/application.yml,file:/opt/phoneclone/application-production.yml --spring.profiles.active=production
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

#### 2. 启动服务

```bash
# 重新加载systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start phoneclone-backend

# 设置开机自启
sudo systemctl enable phoneclone-backend

# 查看状态
sudo systemctl status phoneclone-backend

# 查看日志
sudo journalctl -u phoneclone-backend -f
```

### 第七步：配置Nginx反向代理（可选但推荐）

#### 1. 安装Nginx

```bash
# Ubuntu
sudo apt install -y nginx

# CentOS
sudo yum install -y nginx
```

#### 2. 配置Nginx

```bash
sudo nano /etc/nginx/sites-available/phoneclone
```

内容：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或ECS公网IP

    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Swagger UI
    location /swagger-ui.html {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

#### 3. 启用配置

```bash
# Ubuntu
sudo ln -s /etc/nginx/sites-available/phoneclone /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# CentOS（直接编辑主配置）
sudo nano /etc/nginx/nginx.conf
# 在http块中添加include /etc/nginx/conf.d/*.conf;
sudo cp /etc/nginx/sites-available/phoneclone /etc/nginx/conf.d/phoneclone.conf
sudo nginx -t
sudo systemctl restart nginx
```

### 第八步：配置HTTPS（使用Let's Encrypt）

#### 1. 安装Certbot

```bash
# Ubuntu
sudo apt install -y certbot python3-certbot-nginx

# CentOS
sudo yum install -y certbot python3-certbot-nginx
```

#### 2. 申请SSL证书

```bash
sudo certbot --nginx -d your-domain.com
```

按照提示完成配置。

#### 3. 自动续期

证书会自动续期，也可以手动测试：

```bash
sudo certbot renew --dry-run
```

## 方案B：容器服务ACK部署

### 第一步：准备Docker镜像

#### 1. 创建Dockerfile

在 `backend/` 目录下已有 `Dockerfile`，内容：

```dockerfile
FROM openjdk:21-jdk-slim
WORKDIR /app
COPY build/libs/phoneclone-backend-1.0.0.jar app.jar
EXPOSE 8080
ENV JAVA_OPTS="-Xmx512m -Xms256m"
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
```

#### 2. 构建镜像

```bash
cd backend
./gradlew clean build -x test
docker build -t phoneclone-backend:1.0.0 .
```

#### 3. 推送到阿里云容器镜像服务

1. 登录 [容器镜像服务控制台](https://cr.console.aliyun.com)
2. 创建命名空间和仓库
3. 登录并推送镜像：

```bash
# 登录
docker login --username=你的阿里云账号 registry.cn-hangzhou.aliyuncs.com

# 标记镜像
docker tag phoneclone-backend:1.0.0 registry.cn-hangzhou.aliyuncs.com/你的命名空间/phoneclone-backend:1.0.0

# 推送镜像
docker push registry.cn-hangzhou.aliyuncs.com/你的命名空间/phoneclone-backend:1.0.0
```

### 第二步：创建ACK集群

1. 登录 [容器服务控制台](https://cs.console.aliyun.com)
2. 创建Kubernetes集群
3. 配置集群参数
4. 等待创建完成

### 第三步：部署应用

#### 使用kubectl部署

```bash
# 创建deployment.yaml
cat <<EOF > deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: phoneclone-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phoneclone-backend
  template:
    metadata:
      labels:
        app: phoneclone-backend
    spec:
      containers:
      - name: backend
        image: registry.cn-hangzhou.aliyuncs.com/你的命名空间/phoneclone-backend:1.0.0
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require"
        - name: DB_USERNAME
          value: "postgres"
        - name: DB_PASSWORD
          value: "你的Supabase密码"
        - name: JWT_SECRET
          value: "你的JWT密钥"
        - name: SPRING_PROFILES_ACTIVE
          value: "production"
---
apiVersion: v1
kind: Service
metadata:
  name: phoneclone-backend-service
spec:
  selector:
    app: phoneclone-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
EOF

# 部署
kubectl apply -f deployment.yaml
```

## 环境变量配置

### ECS部署环境变量

在 `application-production.yml` 中配置，或使用环境变量：

```bash
export SPRING_DATASOURCE_URL="jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require"
export DB_USERNAME="postgres"
export DB_PASSWORD="你的Supabase密码"
export JWT_SECRET="你的JWT密钥至少32字符"
export SPRING_PROFILES_ACTIVE="production"
```

### 生成JWT密钥

```bash
# 在服务器上生成
openssl rand -base64 32
```

## 验证部署

### 1. 检查服务状态

```bash
# systemd服务
sudo systemctl status phoneclone-backend

# 查看日志
sudo journalctl -u phoneclone-backend -n 50
```

### 2. 测试API

```bash
# 本地测试
curl http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"12345678"}'

# 公网测试（使用ECS公网IP）
curl http://你的ECS公网IP:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@example.com","password":"12345678"}'
```

### 3. 访问Swagger UI

```
http://你的ECS公网IP:8080/swagger-ui.html
```

## 监控和维护

### 查看应用日志

```bash
# systemd日志
sudo journalctl -u phoneclone-backend -f

# 应用日志文件（如果配置了）
tail -f /opt/phoneclone/logs/application.log
```

### 重启服务

```bash
sudo systemctl restart phoneclone-backend
```

### 更新应用

```bash
# 1. 停止服务
sudo systemctl stop phoneclone-backend

# 2. 备份旧版本
cp /opt/phoneclone/phoneclone-backend-1.0.0.jar /opt/phoneclone/phoneclone-backend-1.0.0.jar.bak

# 3. 上传新版本
scp build/libs/phoneclone-backend-1.0.0.jar root@你的ECS公网IP:/opt/phoneclone/

# 4. 启动服务
sudo systemctl start phoneclone-backend
```

## 安全建议

### 1. 防火墙配置

```bash
# 使用ufw（Ubuntu）
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable

# 使用firewalld（CentOS）
sudo firewall-cmd --permanent --add-port=22/tcp
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload
```

### 2. 使用非root用户运行

```bash
# 创建专用用户
sudo useradd -r -s /bin/false phoneclone

# 修改文件所有者
sudo chown -R phoneclone:phoneclone /opt/phoneclone

# 修改systemd服务文件中的User
sudo nano /etc/systemd/system/phoneclone-backend.service
# 将 User=root 改为 User=phoneclone
```

### 3. 配置日志轮转

创建 `/etc/logrotate.d/phoneclone`:

```
/opt/phoneclone/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0644 phoneclone phoneclone
}
```

## 故障排查

### 问题1：服务无法启动

**检查**：
```bash
# 查看服务状态
sudo systemctl status phoneclone-backend

# 查看详细日志
sudo journalctl -u phoneclone-backend -n 100

# 检查Java版本
java -version

# 检查端口占用
sudo netstat -tulpn | grep 8080
```

### 问题2：数据库连接失败

**检查**：
- Supabase连接信息是否正确
- SSL配置（`sslmode=require`）
- 安全组是否允许出站连接
- 测试连接：`telnet db.xxxxx.supabase.co 5432`

### 问题3：无法访问API

**检查**：
- 安全组是否开放8080端口
- 防火墙规则
- 服务是否运行
- Nginx配置是否正确

## 成本估算

### ECS实例

- **最低配置**（1核2GB）：约 ¥50-100/月
- **推荐配置**（2核4GB）：约 ¥150-200/月
- **带宽费用**：按流量计费，约 ¥0.8/GB

### 容器服务ACK

- **集群费用**：按节点计费
- **节点费用**：同ECS实例

## 推荐配置

### 小型项目（测试/开发）

- ECS：1核2GB
- 系统盘：40GB
- 带宽：1Mbps
- 预计成本：¥50-100/月

### 生产环境

- ECS：2核4GB或更高
- 系统盘：100GB SSD
- 带宽：5Mbps
- 预计成本：¥200-300/月

## 下一步

部署完成后：
1. 配置Android应用的API地址
2. 测试所有功能
3. 配置域名和HTTPS
4. 设置监控和告警

祝部署顺利！🚀

