# 连接问题排查指南

## 🔴 错误信息

```
java.net.ConnectException: Failed to connect to /47.94.122.137:8080
```

## 📋 问题分析

应用能够发送请求，但无法连接到服务器。可能的原因：

1. **阿里云安全组未开放8080端口**
2. **服务器防火墙阻止了8080端口**
3. **后端服务未正常运行**
4. **网络连接问题**

## ✅ 排查步骤

### 第一步：检查后端服务是否运行

在服务器上执行：

```bash
# SSH连接到服务器
ssh root@47.94.122.137

# 检查服务状态
systemctl status phoneclone-backend

# 如果服务未运行，启动服务
systemctl start phoneclone-backend

# 查看服务日志
journalctl -u phoneclone-backend -n 50
```

### 第二步：检查服务器本地连接

在服务器上执行：

```bash
# 测试本地8080端口是否可访问
curl http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'

# 或使用telnet测试端口
telnet localhost 8080
```

### 第三步：检查防火墙配置

在服务器上执行：

```bash
# 检查防火墙状态
ufw status

# 如果防火墙启用，开放8080端口
ufw allow 8080/tcp

# 或临时关闭防火墙测试（不推荐生产环境）
# ufw disable
```

### 第四步：检查阿里云安全组（重要！）

这是最常见的问题！

1. **登录阿里云控制台**
   - 访问：https://ecs.console.aliyun.com

2. **进入ECS实例**
   - ECS → 实例与镜像 → 实例
   - 找到你的服务器实例

3. **配置安全组**
   - 点击实例 → 安全组 → 配置规则
   - 或：网络与安全 → 安全组 → 管理规则

4. **添加入站规则**
   - 点击"添加安全组规则"
   - 配置如下：
     ```
     规则方向：入方向
     授权策略：允许
     协议类型：自定义TCP
     端口范围：8080/8080
     授权对象：0.0.0.0/0（或限制为特定IP）
     描述：PhoneClone后端服务端口
     ```
   - 点击"保存"

5. **验证规则**
   - 确认规则已添加
   - 状态为"生效中"

### 第五步：从外部测试连接

在本地电脑执行：

```bash
# 测试服务器端口是否开放
telnet 47.94.122.137 8080

# 或使用curl测试
curl http://47.94.122.137:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'

# 或使用nc（netcat）
nc -zv 47.94.122.137 8080
```

**如果连接失败**，说明安全组或防火墙配置有问题。

### 第六步：检查服务器监听端口

在服务器上执行：

```bash
# 检查8080端口是否在监听
netstat -tlnp | grep 8080

# 或使用ss命令
ss -tlnp | grep 8080

# 应该看到类似输出：
# tcp  0  0  0.0.0.0:8080  0.0.0.0:*  LISTEN  5443/java
```

**如果看不到8080端口监听**，说明服务没有正常启动。

## 🔧 快速修复命令

### 在服务器上执行：

```bash
# 1. 检查服务状态
systemctl status phoneclone-backend

# 2. 如果服务未运行，启动服务
systemctl start phoneclone-backend

# 3. 开放防火墙端口
ufw allow 8080/tcp

# 4. 检查端口监听
netstat -tlnp | grep 8080

# 5. 测试本地连接
curl http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'
```

### 在阿里云控制台：

1. ECS → 实例 → 你的实例
2. 安全组 → 配置规则
3. 添加入站规则：端口8080，协议TCP，授权对象0.0.0.0/0

## 📝 完整检查清单

- [ ] 后端服务正在运行（`systemctl status phoneclone-backend`）
- [ ] 服务器本地可以访问（`curl http://localhost:8080/api/...`）
- [ ] 防火墙已开放8080端口（`ufw allow 8080/tcp`）
- [ ] 阿里云安全组已配置8080端口入站规则
- [ ] 从外部可以连接（`telnet 47.94.122.137 8080`）
- [ ] 端口正在监听（`netstat -tlnp | grep 8080`）

## ⚠️ 常见问题

### 问题1：安全组配置后仍然无法连接

**解决**：
- 等待1-2分钟让规则生效
- 检查规则是否应用到正确的安全组
- 确认实例绑定的安全组是否正确

### 问题2：服务启动失败

**检查日志**：
```bash
journalctl -u phoneclone-backend -n 100
```

**常见原因**：
- 配置文件错误
- 数据库连接失败
- 端口被占用

### 问题3：防火墙阻止

**检查**：
```bash
# 查看防火墙规则
ufw status numbered

# 删除阻止规则（如果有）
ufw delete [规则编号]

# 添加允许规则
ufw allow 8080/tcp
```

## 🎯 验证修复

修复后，在本地测试：

```bash
# 测试API连接
curl http://47.94.122.137:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'
```

**成功响应**应该返回：
```json
{
  "token": "...",
  "user": {...}
}
```

然后在Android应用中重新测试登录。

---

**最可能的原因**：阿里云安全组未配置8080端口入站规则。请先检查并配置安全组！

