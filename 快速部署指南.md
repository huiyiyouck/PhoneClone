# 快速部署指南 - Supabase + Railway/Render

## 重要说明

**Vercel主要支持Node.js**，对于Spring Boot Java应用支持有限。推荐使用：
- **Railway**（推荐）- 支持Java，部署简单，有免费额度
- **Render**（备选）- 免费套餐，支持Java

## 第一步：部署Supabase数据库（10分钟）

### 1. 创建Supabase项目

1. 访问 https://supabase.com
2. 使用GitHub登录
3. 点击 "New Project"
4. 填写：
   - Name: `phoneclone`
   - Database Password: **设置强密码并保存**
   - Region: 选择最近的区域
5. 点击 "Create new project"
6. 等待创建完成（2-3分钟）

### 2. 获取数据库连接信息

1. 进入项目 → "Settings" → "Database"
2. 找到 "Connection string" → "URI"
3. 复制连接字符串，格式：
   ```
   postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

### 3. 初始化数据库

1. 点击左侧 "SQL Editor"
2. 点击 "New query"
3. 复制 `backend/src/main/resources/db/migration/V1__init.sql` 的内容
4. 粘贴并点击 "Run"

### 4. 创建测试账号

在SQL Editor中执行：

```sql
-- 复制 backend/src/main/resources/db/migration/V2__create_test_users.sql 的内容
-- 或手动执行以下SQL

INSERT INTO users (username, email, password_hash, membership_level, created_at, updated_at)
VALUES ('testuser', 'test@example.com', 'ef797c8118f02dfb649607dd5d3f8c7623048c9c063d532cc95c5ed7a898a64f', 'FREE', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
ON CONFLICT (email) DO NOTHING;
```

## 第二步：部署到Railway（推荐，5分钟）

### 1. 创建Railway项目

1. 访问 https://railway.app
2. 使用GitHub登录
3. 点击 "New Project"
4. 选择 "Deploy from GitHub repo"
5. 选择你的项目仓库

### 2. 配置服务

1. 在项目设置中：
   - **Root Directory**: `backend`
   - **Build Command**: `./gradlew build -x test`
   - **Start Command**: `java -jar build/libs/phoneclone-backend-1.0.0.jar`

### 3. 配置环境变量

在Railway Dashboard → 服务 → "Variables"：

```bash
# 数据库配置（将Supabase连接字符串转换为JDBC格式）
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=你的Supabase密码

# JWT密钥（至少32字符）
JWT_SECRET=你的随机密钥至少32个字符长度

# Spring配置
SPRING_PROFILES_ACTIVE=production
PORT=8080
```

**转换Supabase连接字符串**：
- 原格式：`postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres`
- JDBC格式：`jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?user=postgres&password=[PASSWORD]&sslmode=require`

### 4. 获取部署URL

部署完成后，Railway会提供URL：
```
https://your-app.railway.app
```

## 第三步：部署到Render（备选方案）

### 1. 创建Web Service

1. 访问 https://render.com
2. 使用GitHub登录
3. 点击 "New" → "Web Service"
4. 连接GitHub仓库

### 2. 配置服务

- **Name**: `phoneclone-backend`
- **Root Directory**: `backend`
- **Environment**: `Java`
- **Build Command**: `./gradlew build -x test`
- **Start Command**: `java -jar build/libs/phoneclone-backend-1.0.0.jar`

### 3. 配置环境变量

同Railway配置，在 "Environment" 部分添加。

## 第四步：配置Android应用

### 1. 更新API地址

编辑 `app/src/main/java/com/phoneclone/network/RetrofitClient.java`:

```java
// Railway
private static final String BASE_URL = "https://your-app.railway.app/api/";

// 或 Render
// private static final String BASE_URL = "https://your-app.onrender.com/api/";
```

### 2. 构建APK

```bash
cd app
./gradlew assembleRelease
```

## 测试部署

### 1. 测试后端API

```bash
# 测试注册
curl -X POST https://your-app.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"newuser","email":"new@example.com","password":"12345678"}'

# 测试登录
curl -X POST https://your-app.railway.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'
```

### 2. 访问Swagger UI

```
https://your-app.railway.app/swagger-ui.html
```

### 3. 测试Android应用

1. 安装APK
2. 使用测试账号登录：
   - 邮箱：`test@example.com`
   - 密码：`12345678`

## 环境变量配置模板

### Railway/Render环境变量

```bash
# Supabase数据库连接
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=你的Supabase密码

# JWT配置（生成随机密钥）
JWT_SECRET=使用openssl生成：openssl rand -base64 32

# Spring配置
SPRING_PROFILES_ACTIVE=production
PORT=8080
```

## 生成JWT密钥

```bash
# 使用openssl
openssl rand -base64 32

# 或使用在线工具
# https://www.random.org/strings/
# 长度：32-64字符，随机字符串
```

## 常见问题

### Q1: Railway部署失败

**检查**：
- Java版本（需要Java 21）
- 构建命令是否正确
- 环境变量是否配置
- 查看构建日志

### Q2: 数据库连接失败

**解决**：
- 确认Supabase连接信息正确
- 添加 `sslmode=require` 参数
- 检查密码是否正确

### Q3: API返回404

**解决**：
- 确认URL包含 `/api` 前缀
- 检查后端服务是否运行
- 查看Railway日志

## 详细文档

- [Supabase部署指南.md](Supabase部署指南.md) - Supabase详细配置
- [完整部署手册.md](完整部署手册.md) - 完整部署步骤
- [部署检查清单.md](部署检查清单.md) - 部署检查项

## 完成标志

✅ 可以在Swagger UI访问API文档  
✅ 可以使用curl测试API  
✅ Android应用可以正常登录  
✅ 数据可以正常同步  

**部署完成！** 🎉

