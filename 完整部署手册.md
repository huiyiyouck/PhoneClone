# 完整部署手册 - Supabase + Railway/Render

## 架构概览

```
┌─────────────────┐
│  Android应用     │
│  (客户端)        │
└────────┬────────┘
         │ HTTPS
┌────────▼────────┐
│  Railway/Render  │
│  (后端服务)      │
└────────┬────────┘
         │ JDBC (SSL)
┌────────▼────────┐
│  Supabase        │
│  (PostgreSQL)    │
└─────────────────┘
```

## 第一部分：Supabase数据库部署

### 步骤1：创建Supabase项目

1. 访问 [https://supabase.com](https://supabase.com)
2. 使用GitHub账号登录
3. 点击 "New Project"
4. 填写信息：
   - **Name**: `phoneclone`
   - **Database Password**: 设置强密码（**保存好**）
   - **Region**: 选择最近的区域
5. 点击 "Create new project"
6. 等待创建完成（2-3分钟）

### 步骤2：获取数据库连接信息

1. 进入项目 Dashboard
2. 点击 "Settings" → "Database"
3. 找到 "Connection string" → "URI"
4. 复制连接字符串

**连接信息格式**：
```
postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

**提取的信息**：
- Host: `db.xxxxx.supabase.co`
- Port: `5432`
- Database: `postgres`
- User: `postgres`
- Password: 你设置的密码

### 步骤3：初始化数据库表

1. 在Supabase Dashboard，点击 "SQL Editor"
2. 点击 "New query"
3. 复制并执行 `backend/src/main/resources/db/migration/V1__init.sql` 的内容
4. 点击 "Run" 执行

### 步骤4：创建测试账号

在SQL Editor中执行：

```sql
-- 执行 backend/src/main/resources/db/migration/V2__create_test_users.sql
-- 或手动执行以下SQL

INSERT INTO users (username, email, password_hash, membership_level, created_at, updated_at)
VALUES (
    'testuser',
    'test@example.com',
    'ef797c8118f02dfb649607dd5d3f8c7623048c9c063d532cc95c5ed7a898a64f',
    'FREE',
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
) ON CONFLICT (email) DO NOTHING;

INSERT INTO users (username, email, password_hash, membership_level, membership_expiry, created_at, updated_at)
VALUES (
    'vipuser',
    'vip@example.com',
    'ef797c8118f02dfb649607dd5d3f8c7623048c9c063d532cc95c5ed7a898a64f',
    'VIP',
    CURRENT_TIMESTAMP + INTERVAL '1 month',
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
) ON CONFLICT (email) DO NOTHING;

INSERT INTO users (username, email, password_hash, membership_level, created_at, updated_at)
VALUES (
    'admin',
    'admin@example.com',
    '240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9',
    'SVIP',
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
) ON CONFLICT (email) DO NOTHING;
```

### 步骤5：配置连接池（可选但推荐）

1. 在 "Settings" → "Database" → "Connection Pooling"
2. 启用连接池
3. 使用连接池的连接字符串（端口通常是6543）

## 第二部分：Railway部署后端服务（推荐）

### 步骤1：准备项目

确保 `backend` 目录包含：
- `build.gradle`
- `src/` 目录
- `gradlew` 和 `gradlew.bat`

### 步骤2：部署到Railway

#### 方法A：使用Web界面（推荐）

1. 访问 [https://railway.app](https://railway.app)
2. 使用GitHub账号登录
3. 点击 "New Project"
4. 选择 "Deploy from GitHub repo"
5. 选择你的项目仓库
6. 配置：
   - **Root Directory**: `backend`
   - **Build Command**: `./gradlew build -x test`
   - **Start Command**: `java -jar build/libs/phoneclone-backend-1.0.0.jar`

#### 方法B：使用Railway CLI

```bash
# 安装Railway CLI
npm i -g @railway/cli

# 登录
railway login

# 初始化项目
cd backend
railway init

# 部署
railway up
```

### 步骤3：配置环境变量

在Railway Dashboard中：

1. 进入项目 → 选择服务
2. 点击 "Variables" 标签
3. 添加以下环境变量：

```bash
# 数据库连接（使用Supabase连接字符串转换）
DATABASE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?user=postgres&password=YOUR-PASSWORD&sslmode=require

# 或分别设置
DB_USERNAME=postgres
DB_PASSWORD=YOUR-SUPABASE-PASSWORD
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require

# JWT配置
JWT_SECRET=your-256-bit-secret-key-change-this-in-production-min-32-chars

# Spring配置
SPRING_PROFILES_ACTIVE=production
PORT=8080
```

**重要**：
- 将 `YOUR-PASSWORD` 替换为Supabase数据库密码
- 将 `YOUR-SUPABASE-PASSWORD` 替换为实际密码
- `JWT_SECRET` 必须是至少32个字符的随机字符串

### 步骤4：获取部署URL

部署完成后，Railway会提供URL：
```
https://your-app.railway.app
```

### 步骤5：验证部署

```bash
# 测试健康检查（如果配置了）
curl https://your-app.railway.app/api/health

# 测试注册接口
curl -X POST https://your-app.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser2",
    "email": "test2@example.com",
    "password": "12345678"
  }'

# 访问Swagger UI
# https://your-app.railway.app/swagger-ui.html
```

## 第三部分：Render部署（备选方案）

### 步骤1：创建Web Service

1. 访问 [https://render.com](https://render.com)
2. 使用GitHub账号登录
3. 点击 "New" → "Web Service"
4. 连接GitHub仓库

### 步骤2：配置服务

- **Name**: `phoneclone-backend`
- **Root Directory**: `backend`
- **Environment**: `Java`
- **Build Command**: `./gradlew build -x test`
- **Start Command**: `java -jar build/libs/phoneclone-backend-1.0.0.jar`

### 步骤3：配置环境变量

同Railway配置，在Render Dashboard的 "Environment" 部分添加。

### 步骤4：部署

点击 "Save Changes"，Render会自动部署。

## 第四部分：配置Android应用

### 步骤1：更新API地址

编辑 `app/src/main/java/com/phoneclone/network/RetrofitClient.java`:

```java
// 生产环境 - Railway
private static final String BASE_URL = "https://your-app.railway.app/api/";

// 或 Render
// private static final String BASE_URL = "https://your-app.onrender.com/api/";
```

### 步骤2：构建APK

```bash
cd app
./gradlew assembleRelease
```

### 步骤3：测试应用

1. 安装APK
2. 使用测试账号登录：
   - 邮箱：`test@example.com`
   - 密码：`12345678`

## 环境变量配置参考

### Supabase数据库连接字符串转换

**Supabase URI格式**：
```
postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

**转换为JDBC格式**：
```
jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?user=postgres&password=[PASSWORD]&sslmode=require
```

**或使用环境变量**：
```bash
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=[PASSWORD]
```

### 完整环境变量列表

```bash
# 数据库配置
SPRING_DATASOURCE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=your-supabase-password

# JWT配置
JWT_SECRET=your-256-bit-secret-key-minimum-32-characters-long

# Spring配置
SPRING_PROFILES_ACTIVE=production
PORT=8080

# 日志配置（可选）
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_PHONECLONE=DEBUG
```

## 测试清单

### ✅ 数据库测试

```sql
-- 在Supabase SQL Editor中执行
SELECT COUNT(*) FROM users;
SELECT * FROM users LIMIT 5;
```

### ✅ 后端API测试

```bash
# 1. 测试注册
curl -X POST https://your-app.railway.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"newuser","email":"new@example.com","password":"12345678"}'

# 2. 测试登录
curl -X POST https://your-app.railway.app/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'

# 3. 访问Swagger UI
# 浏览器打开：https://your-app.railway.app/swagger-ui.html
```

### ✅ Android应用测试

1. 安装APK
2. 打开应用
3. 使用测试账号登录
4. 测试各项功能

## 故障排查

### 问题1：数据库连接失败

**症状**：后端启动失败，日志显示连接错误

**解决**：
1. 检查Supabase连接信息是否正确
2. 确认密码正确
3. 检查SSL配置（`sslmode=require`）
4. 确认防火墙允许连接

### 问题2：Railway部署失败

**症状**：构建失败或启动失败

**解决**：
1. 检查构建日志
2. 确认 `build.gradle` 配置正确
3. 检查Java版本（需要Java 21）
4. 确认环境变量配置正确

### 问题3：API无法访问

**症状**：Android应用无法连接后端

**解决**：
1. 检查Railway服务是否运行
2. 确认API地址配置正确
3. 检查HTTPS证书
4. 查看Railway日志

### 问题4：表不存在错误

**症状**：API返回数据库错误

**解决**：
1. 在Supabase SQL Editor中执行初始化脚本
2. 检查Flyway迁移是否成功
3. 手动创建表结构

## 监控和维护

### Railway监控

1. 访问Railway Dashboard
2. 查看服务指标：
   - CPU使用率
   - 内存使用
   - 请求数
   - 错误率
3. 查看日志：点击服务 → "View Logs"

### Supabase监控

1. 访问Supabase Dashboard
2. 查看数据库指标：
   - 连接数
   - 查询性能
   - 存储使用
3. 查看日志：点击 "Logs"

### 定期维护

1. **数据库备份**：Supabase自动备份，也可手动导出
2. **日志清理**：定期清理应用日志
3. **依赖更新**：定期更新依赖库
4. **安全审计**：检查环境变量和密钥

## 成本估算

### Supabase

- **免费套餐**：
  - 500MB数据库
  - 2GB带宽
  - 50,000月活跃用户
  - 足够小规模使用

- **Pro套餐**：$25/月（如需要更多资源）

### Railway

- **免费套餐**：
  - $5免费额度/月
  - 足够小规模使用

- **Pro套餐**：$20/月（如需要更多资源）

### Render

- **免费套餐**：
  - 750小时/月
  - 自动休眠（15分钟无活动）
  - 适合开发测试

- **Starter套餐**：$7/月（保持运行）

## 总结

部署完成后，你将拥有：

1. ✅ Supabase PostgreSQL数据库（云数据库）
2. ✅ Railway/Render后端服务（自动HTTPS）
3. ✅ Android应用（配置了生产API地址）

**测试账号**：
- 邮箱：`test@example.com`
- 密码：`12345678`

**API地址示例**：
- Railway: `https://your-app.railway.app/api/`
- Render: `https://your-app.onrender.com/api/`

祝部署顺利！🚀

