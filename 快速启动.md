# 快速启动指南 - 混合架构方案

## 架构说明

本应用采用**混合架构**方案：
- **Android应用**：客户端，通过HTTP API与后端通信
- **Spring Boot后端**：处理业务逻辑、认证、数据同步
- **PostgreSQL数据库**：数据存储

## 一键启动检查清单

### ✅ 后端服务启动步骤

1. **确保PostgreSQL已安装并运行**
   ```bash
   # 检查PostgreSQL是否运行
   psql --version
   ```

2. **创建数据库**
   ```sql
   psql -U postgres
   CREATE DATABASE phoneclone_db;
   \q
   ```

3. **配置数据库连接**
   编辑 `backend/src/main/resources/application.yml`:
   ```yaml
   spring:
     datasource:
       url: jdbc:postgresql://localhost:5432/phoneclone_db
       username: postgres  # 你的PostgreSQL用户名
       password: your_password  # 你的PostgreSQL密码
   ```

4. **启动后端**
   ```bash
   cd backend
   ./gradlew bootRun
   ```
   
   看到 "Started PhoneCloneBackendApplication" 表示启动成功！

5. **测试后端**
   打开浏览器访问: http://localhost:8080/swagger-ui.html

### ✅ Android客户端启动步骤

1. **配置API地址**
   
   编辑 `app/src/main/java/com/phoneclone/network/RetrofitClient.java`:
   
   - **开发环境（本地后端）**:
     - Android模拟器: `http://10.0.2.2:8080/api/`
     - 真机: `http://你的电脑IP:8080/api/` (如: `http://192.168.1.100:8080/api/`)
   - **生产环境**: `https://your-api-domain.com/api/`

2. **在Android Studio中打开项目**
   - File > Open > 选择项目目录
   - 等待Gradle同步完成

3. **运行应用**
   - 连接设备或启动模拟器
   - 点击绿色运行按钮 ▶️
   - 或按快捷键 `Shift+F10`

## 快速测试

### 测试注册功能
1. 打开应用
2. 点击"立即注册"
3. 填写：
   - 用户名: test
   - 邮箱: test@example.com
   - 密码: 12345678
4. 点击注册

### 测试登录功能
1. 使用注册的邮箱和密码登录
2. 登录成功后进入主页

### 测试应用列表
1. 点击"添加应用"按钮
2. 等待应用列表加载
3. 选择几个应用
4. 点击右下角"+"按钮

## 常见问题快速解决

### ❌ 后端启动失败

**错误**: `Connection refused` 或 `database does not exist`
- ✅ 检查PostgreSQL是否运行: `pg_isready`
- ✅ 检查数据库是否创建
- ✅ 检查用户名密码是否正确

**错误**: `Port 8080 already in use`
- ✅ 修改 `application.yml` 中的端口: `server.port: 8081`

### ❌ Android连接失败

**错误**: `Failed to connect to /10.0.2.2:8080`
- ✅ 确保后端服务正在运行
- ✅ 检查API地址配置
- ✅ 真机需要确保手机和电脑在同一WiFi网络

**错误**: `Network security config`
- ✅ 已在AndroidManifest.xml中配置 `usesCleartextTraffic="true"`

### ❌ 编译错误

**错误**: Hilt相关
- ✅ 清理项目: `Build > Clean Project`
- ✅ 重新构建: `Build > Rebuild Project`

**错误**: Room数据库
- ✅ 确保数据库版本号正确
- ✅ 如果修改了实体，增加版本号

## 下一步

项目已可以运行！接下来可以：
1. 测试所有功能
2. 集成VirtualApp SDK实现多开
3. 完善数据同步功能
4. 优化UI和用户体验

祝开发顺利！🚀

