# 本地配置文件说明

## 📝 已创建的配置文件

已在本地项目目录创建了生产环境配置文件：

```
backend/application-production.yml
```

## ✅ 文件位置

```
/Users/chengkang/Project/Project_Android/PhoneClone/backend/application-production.yml
```

## 🔒 安全保护

此文件**已配置为不会提交到GitHub**：

1. ✅ 已在 `.gitignore` 中排除：
   ```
   application-production.yml
   ```

2. ✅ 验证方法：
   ```bash
   cd /Users/chengkang/Project/Project_Android/PhoneClone
   git status
   # 不会显示 application-production.yml
   ```

## 📋 需要填写的配置

### 1. Supabase数据库连接信息

**获取方法**：
1. 访问：https://supabase.com/dashboard
2. 选择你的项目
3. Settings → Database → Connection string
4. 选择 **JDBC** 格式

**填写内容**：
```yaml
spring:
  datasource:
    url: jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 你的Supabase数据库密码
```

### 2. JWT密钥

**生成命令**：
```bash
openssl rand -base64 32
```

**填写内容**：
```yaml
spring:
  security:
    jwt:
      secret: 生成的JWT密钥（至少32字符）
```

## 📤 上传到服务器

配置完成后，上传到阿里云服务器：

```bash
# 在本地执行
scp backend/application-production.yml root@你的服务器IP:/opt/phoneclone/backend/application-production.yml

# 在服务器上设置权限
ssh root@你的服务器IP "chmod 600 /opt/phoneclone/backend/application-production.yml"
```

## 🔍 验证配置

### 检查文件是否存在

```bash
ls -la backend/application-production.yml
```

### 检查是否会被提交到GitHub

```bash
cd /Users/chengkang/Project/Project_Android/PhoneClone
git status backend/application-production.yml
# 应该显示：nothing to commit（文件被忽略）
```

### 查看配置内容（不显示敏感信息）

```bash
cat backend/application-production.yml | grep -v password | grep -v secret
```

## ⚠️ 重要提示

1. **不要提交到GitHub**：
   - 文件已在 `.gitignore` 中
   - 包含敏感信息（数据库密码、JWT密钥）

2. **本地使用**：
   - 可以用于本地测试
   - 确保配置正确后再上传到服务器

3. **服务器配置**：
   - 上传后设置权限：`chmod 600`
   - 确保文件路径正确：`/opt/phoneclone/backend/application-production.yml`

## 📝 编辑配置文件

```bash
# 使用nano编辑器
nano backend/application-production.yml

# 或使用其他编辑器
code backend/application-production.yml  # VS Code
vim backend/application-production.yml   # Vim
```

## ✅ 完成标志

配置完成后，你应该：
1. ✅ 文件存在于 `backend/application-production.yml`
2. ✅ 已填写Supabase连接信息
3. ✅ 已生成并填写JWT密钥
4. ✅ 文件不会被提交到GitHub（已验证）
5. ✅ 可以上传到服务器使用

---

**总结**：配置文件已创建在本地，填写信息后即可上传到服务器使用。

