# 配置文件设置指南

## 📝 本地配置 application-production.yml

### 步骤1：创建配置文件

在本地项目目录中：

```bash
cd /Users/chengkang/Project/Project_Android/PhoneClone/backend

# 复制模板文件
cp application-production.yml.template application-production.yml

# 编辑配置文件
nano application-production.yml
# 或使用其他编辑器
```

### 步骤2：填写配置信息

#### 2.1 获取Supabase数据库连接信息

1. **访问Supabase控制台**：
   - 打开：https://supabase.com/dashboard
   - 登录并选择你的项目

2. **获取连接信息**：
   - 点击左侧菜单 **Settings** → **Database**
   - 找到 **Connection string** 部分
   - 选择 **URI** 或 **JDBC** 格式

3. **提取信息**：
   ```
   URL格式：jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
   用户名：postgres
   密码：在 Supabase Settings → Database → Database password 中查看
   ```

#### 2.2 生成JWT密钥

```bash
# 在本地终端执行
openssl rand -base64 32

# 复制生成的密钥，例如：
# aBcDeFgHiJkLmNoPqRsTuVwXyZ1234567890==
```

#### 2.3 填写完整配置

```yaml
spring:
  datasource:
    url: jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 你的实际Supabase密码
  security:
    jwt:
      secret: 你生成的JWT密钥（至少32字符）
server:
  port: 8080
  servlet:
    context-path: /api
```

### 步骤3：验证配置文件

```bash
# 检查文件是否存在
ls -la backend/application-production.yml

# 查看文件内容（确认敏感信息已填写）
cat backend/application-production.yml | grep -v password | grep -v secret
```

## 📤 上传到阿里云服务器

### 方法1：使用scp上传（推荐）

```bash
# 在本地执行（替换为你的服务器IP）
scp backend/application-production.yml root@你的服务器IP:/opt/phoneclone/backend/application-production.yml

# 例如：
scp backend/application-production.yml root@47.xxx.xxx.xxx:/opt/phoneclone/backend/application-production.yml
```

### 方法2：使用sftp上传

```bash
# 在本地执行
sftp root@你的服务器IP

# 在sftp会话中
cd /opt/phoneclone/backend
put backend/application-production.yml application-production.yml
exit
```

### 方法3：手动创建（在服务器上）

```bash
# SSH连接到服务器
ssh root@你的服务器IP

# 创建配置文件
nano /opt/phoneclone/backend/application-production.yml

# 粘贴配置内容，保存退出
```

## ✅ 验证上传成功

在服务器上执行：

```bash
# 检查文件是否存在
ls -la /opt/phoneclone/backend/application-production.yml

# 查看文件内容（确认配置正确）
cat /opt/phoneclone/backend/application-production.yml

# 验证文件权限
chmod 600 /opt/phoneclone/backend/application-production.yml
```

## 🔒 安全提示

1. **不要提交到GitHub**：
   - `application-production.yml` 已在 `.gitignore` 中
   - 确保不会意外提交敏感信息

2. **文件权限**：
   ```bash
   # 在服务器上设置严格权限
   chmod 600 /opt/phoneclone/backend/application-production.yml
   ```

3. **备份配置**：
   ```bash
   # 在服务器上备份
   cp /opt/phoneclone/backend/application-production.yml /root/phoneclone-config-backup.yml
   ```

## 📋 完整操作流程

### 在本地：

```bash
# 1. 进入项目目录
cd /Users/chengkang/Project/Project_Android/PhoneClone/backend

# 2. 复制模板
cp application-production.yml.template application-production.yml

# 3. 编辑配置
nano application-production.yml
# 填写Supabase连接信息和JWT密钥

# 4. 验证配置
cat application-production.yml | grep -E "url|username|password|secret"
```

### 上传到服务器：

```bash
# 方法1：使用scp（最简单）
scp application-production.yml root@你的服务器IP:/opt/phoneclone/backend/

# 方法2：如果服务器上还没有目录，先创建
ssh root@你的服务器IP "mkdir -p /opt/phoneclone/backend"
scp application-production.yml root@你的服务器IP:/opt/phoneclone/backend/
```

### 在服务器上验证：

```bash
# SSH连接到服务器
ssh root@你的服务器IP

# 检查文件
ls -la /opt/phoneclone/backend/application-production.yml

# 设置权限
chmod 600 /opt/phoneclone/backend/application-production.yml

# 验证配置
cat /opt/phoneclone/backend/application-production.yml
```

## 🎯 快速命令（一键上传）

```bash
# 在本地执行（替换为你的服务器IP）
SERVER_IP="你的服务器IP"
scp backend/application-production.yml root@${SERVER_IP}:/opt/phoneclone/backend/application-production.yml && \
ssh root@${SERVER_IP} "chmod 600 /opt/phoneclone/backend/application-production.yml && echo '配置文件上传成功'"
```

## ⚠️ 常见问题

### 问题1：找不到Supabase密码

**解决**：
1. Supabase Dashboard → Settings → Database
2. 如果忘记了密码，可以重置：
   - Settings → Database → Reset database password

### 问题2：连接失败

**检查**：
1. URL是否正确（包含 `?sslmode=require`）
2. 密码是否正确
3. Supabase项目是否启用
4. 网络连接是否正常

### 问题3：JWT密钥格式错误

**解决**：
```bash
# 重新生成JWT密钥
openssl rand -base64 32

# 确保密钥至少32字符
# 直接使用生成的字符串，不需要额外处理
```

---

**总结**：
1. 在本地创建 `application-production.yml`
2. 填写Supabase连接信息和JWT密钥
3. 使用 `scp` 上传到服务器 `/opt/phoneclone/backend/`
4. 设置文件权限为 600

