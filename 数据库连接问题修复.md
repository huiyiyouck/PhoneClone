# 数据库连接问题修复

## 🔴 错误信息

```
org.postgresql.util.PSQLException: The connection attempt failed.
Caused by: java.net.SocketException: Network is unreachable
```

## 📋 问题原因

数据库连接配置格式错误。当前配置文件中URL格式不正确：

**错误的格式**：
```yaml
url: jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres?user=postgres&password=xxx
```

**问题**：
1. URL中不应该包含用户名和密码
2. 缺少 `sslmode=require` 参数（Supabase需要SSL）
3. 用户名和密码应该使用单独的字段

## ✅ 修复步骤

### 在服务器上修复配置文件

```bash
# 编辑配置文件
nano /opt/phoneclone/backend/application-production.yml
```

### 正确的配置格式

```yaml
spring:
  datasource:
    url: jdbc:postgresql://db.dzxunppummwzfkdhlelh.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: cqcet.19981025CK
```

**重要**：
- URL中**不要**包含 `user=` 和 `password=`
- 必须包含 `?sslmode=require`（Supabase要求SSL连接）
- 用户名和密码使用单独的 `username` 和 `password` 字段

### 完整配置文件示例

```yaml
spring:
  datasource:
    url: jdbc:postgresql://db.dzxunppummwzfkdhlelh.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: cqcet.19981025CK
    
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  
  security:
    jwt:
      secret: 你的JWT密钥（至少32字符）
      expiration: 86400000

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.phoneclone: INFO
    org.springframework.security: WARN
    org.springframework.web: INFO
```

## 🔧 修复后重启服务

```bash
# 1. 停止服务
systemctl stop phoneclone-backend

# 2. 验证配置文件格式
cat /opt/phoneclone/backend/application-production.yml | grep -A 3 datasource

# 3. 测试数据库连接（可选）
# 安装PostgreSQL客户端
apt install -y postgresql-client

# 测试连接
psql "host=db.dzxunppummwzfkdhlelh.supabase.co port=5432 dbname=postgres user=postgres password=cqcet.19981025CK sslmode=require"

# 4. 启动服务
systemctl start phoneclone-backend

# 5. 查看日志确认连接成功
journalctl -u phoneclone-backend -f
```

## ✅ 验证修复

修复后，日志应该显示：

```
INFO  --- HikariPool-1 - Start completed.
INFO  --- Tomcat started on port(s): 8080 (http)
INFO  --- Started PhoneCloneBackendApplication
```

然后检查端口：

```bash
netstat -tlnp | grep 8080
```

应该看到：
```
tcp  0  0  0.0.0.0:8080  0.0.0.0:*  LISTEN  [PID]/java
```

## ⚠️ 重要提示

1. **URL格式**：`jdbc:postgresql://host:port/database?sslmode=require`
2. **不要**在URL中包含用户名和密码
3. **必须**包含 `sslmode=require`（Supabase要求）
4. 用户名和密码使用单独的字段

---

**修复配置文件后，服务应该可以正常启动并监听8080端口！**

