# 操作手册 - 混合架构方案

## 用户操作指南

### 首次使用

#### 1. 安装应用

1. 下载APK文件
2. 在Android设备上安装
3. 允许安装未知来源应用（如需要）

#### 2. 注册账号

1. 打开应用
2. 点击"立即注册"
3. 填写信息：
   - 用户名：至少3个字符
   - 邮箱：有效的邮箱地址
   - 密码：至少8个字符
   - 确认密码：与密码一致
4. 点击"注册"
5. 注册成功后自动登录

#### 3. 登录账号

1. 打开应用
2. 输入邮箱和密码
3. 点击"登录"
4. 登录成功后进入主页

**测试账号**：
- 邮箱：`test@example.com`
- 密码：`12345678`

### 日常使用

#### 添加应用

1. 在主页点击"添加应用"按钮
2. 等待应用列表加载
3. 选择需要多开的应用（可多选）
4. 点击右下角"+"按钮确认添加

#### 创建多开实例

1. 在主页找到已添加的应用
2. 点击应用卡片
3. 点击"添加实例"按钮
4. 输入实例名称（如：工作账号、小号等）
5. 选择实例颜色标识
6. 点击"确定"创建实例

#### 启动/停止实例

1. 在应用实例列表中
2. 点击实例右侧的播放按钮启动实例
3. 点击停止按钮停止运行中的实例

#### 管理应用和实例

**删除应用**：
1. 长按应用卡片
2. 选择"删除"选项
3. 确认删除

**删除实例**：
1. 在实例列表中
2. 长按实例项
3. 选择"删除"
4. 确认删除

### 常见问题

#### Q1: 无法登录

**可能原因**：
- 邮箱或密码错误
- 后端服务未运行
- 网络问题

**解决方法**：
1. 检查邮箱和密码是否正确
2. 检查网络连接
3. 检查后端服务状态
4. 尝试重置密码（如支持）

#### Q2: 应用列表加载失败

**可能原因**：
- 权限未授予
- 系统限制

**解决方法**：
1. 检查应用权限设置
2. 授予"查询所有应用"权限
3. 重启应用

#### Q3: 数据同步失败

**解决方法**：
1. 检查网络连接
2. 检查后端服务是否运行
3. 尝试手动同步
4. 联系技术支持

## 管理员操作指南

### 后端服务管理

#### 启动服务

```bash
# systemd服务
sudo systemctl start phoneclone-backend

# Docker
docker start phoneclone-backend

# 直接运行
java -jar phoneclone-backend-1.0.0.jar
```

#### 停止服务

```bash
# systemd服务
sudo systemctl stop phoneclone-backend

# Docker
docker stop phoneclone-backend
```

#### 查看服务状态

```bash
# systemd服务
sudo systemctl status phoneclone-backend

# Docker
docker ps | grep phoneclone-backend
```

#### 查看日志

```bash
# systemd服务
sudo journalctl -u phoneclone-backend -f

# Docker
docker logs -f phoneclone-backend
```

### 数据库管理

#### 查看用户列表

```sql
SELECT id, username, email, membership_level, created_at 
FROM users 
ORDER BY created_at DESC;
```

#### 查看应用统计

```sql
SELECT 
    u.username,
    COUNT(DISTINCT a.id) as app_count,
    COUNT(DISTINCT ai.id) as instance_count
FROM users u
LEFT JOIN apps a ON u.id = a.user_id
LEFT JOIN app_instances ai ON a.id = ai.app_id
GROUP BY u.id, u.username;
```

#### 创建测试账号

```sql
-- 基础测试账号
INSERT INTO users (username, email, password_hash, membership_level, created_at, updated_at)
VALUES (
    'testuser',
    'test@example.com',
    'ef797c8118f02dfb649607dd5d3f8c7623048c9c063d532cc95c5ed7a898a64f',  -- 12345678的SHA-256
    'FREE',
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
) ON CONFLICT (email) DO NOTHING;
```

#### 修改用户会员等级

```sql
UPDATE users 
SET membership_level = 'VIP', 
    membership_expiry = CURRENT_TIMESTAMP + INTERVAL '1 month'
WHERE email = 'user@example.com';
```

#### 备份数据库

```bash
# 备份
pg_dump -h your-db-host -U your_username -d phoneclone_db > backup_$(date +%Y%m%d).sql

# 恢复
psql -h your-db-host -U your_username -d phoneclone_db < backup_20240101.sql
```

### 性能监控

#### 查看API访问统计

访问Swagger UI：`http://your-server:8080/swagger-ui.html`

#### 查看数据库连接

```sql
SELECT count(*) FROM pg_stat_activity;
```

#### 查看慢查询

```sql
SELECT 
    query,
    calls,
    total_time,
    mean_time
FROM pg_stat_statements
ORDER BY mean_time DESC
LIMIT 10;
```

### 维护操作

#### 清理过期会话

```sql
DELETE FROM user_sessions 
WHERE expires_at < CURRENT_TIMESTAMP;
```

#### 优化数据库

```sql
ANALYZE users;
ANALYZE apps;
ANALYZE app_instances;
REINDEX DATABASE phoneclone_db;
```

## 测试账号

### 测试账号列表

| 用户名 | 邮箱 | 密码 | 会员等级 | 用途 |
|--------|------|------|----------|------|
| testuser | test@example.com | 12345678 | FREE | 基础功能测试 |
| vipuser | vip@example.com | 12345678 | VIP | VIP功能测试 |
| admin | admin@example.com | admin123 | SVIP | 管理员测试 |

这些账号会在执行数据库迁移脚本时自动创建。

## 技术支持

如遇到问题，请提供：
1. 错误信息截图
2. 操作步骤
3. 设备信息
4. 应用版本号
5. 后端服务日志

联系方式：
- 技术支持邮箱：support@example.com
- 问题反馈：通过应用内"反馈"功能
