# 快速连接服务器步骤

## 🎯 目标
配置Android应用连接到阿里云服务器后端。

## 📋 第一步：获取服务器地址

### 在服务器上执行：

```bash
# 获取公网IP
curl ifconfig.me

# 或查看服务器信息
hostname -I
```

**记录你的公网IP**：例如 `47.xxx.xxx.xxx`

## 🔧 第二步：修改Android应用配置

### 1. 编辑 RetrofitClient.java

文件位置：
```
app/src/main/java/com/phoneclone/network/RetrofitClient.java
```

修改这一行：
```java
// 将：
private static final String BASE_URL = "http://YOUR_SERVER_IP:8080/api/";

// 改为你的实际IP，例如：
private static final String BASE_URL = "http://47.xxx.xxx.xxx:8080/api/";
```

**注意**：
- 确保包含 `/api/` 后缀
- 如果配置了域名，使用域名：`http://your-domain.com:8080/api/`
- 如果配置了HTTPS，使用：`https://your-domain.com/api/`

## 🏗️ 第三步：构建APK

### 方法1：Android Studio（推荐）

1. 打开项目
2. 点击 **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
3. APK位置：`app/build/outputs/apk/debug/app-debug.apk`

### 方法2：命令行

```bash
cd /Users/chengkang/Project/Project_Android/PhoneClone
./gradlew assembleDebug
```

APK位置：`app/build/outputs/apk/debug/app-debug.apk`

## 📱 第四步：安装APK

### 方法1：USB安装

```bash
# 连接手机后
adb install app/build/outputs/apk/debug/app-debug.apk
```

### 方法2：直接传输

1. 将APK文件传输到手机
2. 在手机上点击安装

## ✅ 第五步：检查防火墙

### 1. 阿里云安全组

在阿里云控制台：
- ECS → 实例 → 安全组 → 配置规则
- 添加入站规则：端口8080，协议TCP，授权对象0.0.0.0/0

### 2. 服务器防火墙

```bash
# 检查防火墙
ufw status

# 开放8080端口（如果启用了防火墙）
ufw allow 8080/tcp
```

## 🧪 第六步：测试连接

### 1. 测试后端API

```bash
# 在服务器上测试
curl http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'

# 从外部测试（使用公网IP）
curl http://你的公网IP:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"12345678"}'
```

### 2. 在Android应用中测试

1. 打开应用
2. 使用测试账号登录：
   - 邮箱：`test@example.com`
   - 密码：`12345678`

## 📝 测试账号

| 邮箱 | 密码 | 会员等级 |
|------|------|----------|
| test@example.com | 12345678 | FREE |
| vip@example.com | 12345678 | VIP |
| admin@example.com | admin123 | SVIP |

## ⚠️ 常见问题

### 问题1：无法连接

**检查**：
- ✅ API地址是否正确（包含 `/api/`）
- ✅ 服务器IP是否正确
- ✅ 端口8080是否开放
- ✅ 安全组是否配置

### 问题2：网络超时

**检查**：
- 服务器是否运行：`systemctl status phoneclone-backend`
- 查看日志：`journalctl -u phoneclone-backend -n 50`

---

**完成！** 按照以上步骤操作即可。

